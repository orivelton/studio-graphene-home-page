import React from 'react';
import Title from '../../components/Title';

const Community = () => (
  <section className="community">
    <Title text={'MEET OUR COMMUNITY'} textBottom={'Know more'} />
  </section>
);

export default Community;
